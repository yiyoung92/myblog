import { DefaultTheme } from 'vitepress';

declare function calculateSidebar(targets?: Array<string | {
    folderName: string;
    separate: boolean;
}>, base?: string, skipLevelsConfig?: Record<string, number>): DefaultTheme.SidebarItem[] | DefaultTheme.SidebarMulti;

export { calculateSidebar };
